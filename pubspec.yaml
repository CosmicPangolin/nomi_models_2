name: nomi_models_2
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com
# author: Wes <email@example.com>

environment:
  sdk: '>=2.1.0 <3.0.0'

dependencies:
  built_collection:
  built_value:
#  path: ^1.4.1

dev_dependencies:
  build_runner:
  built_value_generator:
  pedantic: ^1.0.0
  test: ^1.0.0
